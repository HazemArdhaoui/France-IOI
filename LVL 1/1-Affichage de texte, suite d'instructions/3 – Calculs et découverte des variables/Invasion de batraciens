#include <stdio.h>
#define repeat(nb) for (int _loop = 1, _max = (nb) ; _loop <= _max ; _loop++)
int i=1337;
int main()
{
repeat(12){
       i*=2;}
        printf("%d est,",i);
        
        
        }
        